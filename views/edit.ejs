<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit your chat</title>
</head>

<body>
    <h3>Edit your chat <%= chat.id %>
    </h3>
    <p>This chat was sent from <b>
            <%= chat.from %>
        </b> to <b>
            <%= chat.to%>
        </b></p>
    <form method="POST" action="/chats/<%= chat._id%>?_method=PUT">
        <textarea name="message" rows="10" cols="30"><%= chat.message %></textarea>
        <br>
        <button>edit</button>
    </form>

</body>

</html>